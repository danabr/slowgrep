Communicate between tasks Erlang-style, by only communicating through messages.

The worker tasks claim paths from the orchestrating process until the orchestrating
task says there are no more paths to claim.
